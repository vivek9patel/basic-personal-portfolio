# OS Simulation (Archived)

This folder contains the legacy portfolio shell components — navigation header, custom cursor, floating action button group, Ubuntu "Boot" footer CTA, multi-theme palette selector, and related UI — that powered the previous site layout. They were deprecated in favor of a minimal, text-first, projects-first portfolio design. Tars, the theme toggle, and the like-counter were intentionally excluded and remain active in `/components`.
